import { Cinzel, DM_Serif_Display } from 'next/font/google'
 
export const cinzel = Cinzel({
  subsets: ['latin'],
  display: 'swap',
})
 
export const dm = DM_Serif_Display({
  subsets: ['latin'],
  display: 'swap',
  weight: ['400'],
})